name = "reshi-eiyou-gemini"
main = "worker.js"
compatibility_date = "2024-11-01"

# 使うモデルを変えたい場合はコメントを外して指定
# （無料枠で画像を扱えるモデル。うまくいかない時は gemini-2.5-flash 等に変更）
# [vars]
# MODEL = "gemini-2.0-flash"

# APIキーは vars ではなく secret で登録する（下記コマンド）:
#   npx wrangler secret put GEMINI_API_KEY
