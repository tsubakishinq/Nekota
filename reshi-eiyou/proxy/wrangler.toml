name = "reshi-eiyou-proxy"
main = "worker.js"
compatibility_date = "2024-11-01"

# 使うモデルを変えたい場合はコメントを外して指定
# （安く抑えたいなら claude-haiku-4-5、精度重視なら claude-opus-4-8）
# [vars]
# MODEL = "claude-opus-4-8"

# APIキーは vars ではなく secret で登録する（下記コマンド）:
#   npx wrangler secret put ANTHROPIC_API_KEY
